name: Matrix

on: workflow_dispatch

jobs:
  Matrix:
    runs-on: ${{ matrix.anil }}
    strategy:
      matrix:
        anil: [ubuntu-latest, mac-latest]
    steps:
      - name: Print OS
        run: echo "Running on... ${{ matrix.anil }}"
