name: Matrix OS and Version

on: workflow_dispatch

jobs:
  Matrix-OS-Versions:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        version: [11, 18]

    steps:
      - name: print the OS
        run: echo "Running on ${{ matrix.os }}"

      - name: Print Versions
        run: echo "Versions ${{ matrix.version }}"
