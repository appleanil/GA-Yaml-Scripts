name: Simple Checkout GitHub Actions

on:
  workflow_dispatch:

jobs:
  Checkout-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Create a file
        run: echo "Hi, I'm Dhage Anil Kumar and I'm learning GitHub Actions with DevOps" > anil.txt

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show Files
        run: ls -lrth

      - name: Display Repository File
        run: Github-Actions-practice.txt
