name: Simple Checkout jobs

on: workflow_dispatch

jobs:
  Checkout-Job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Show the file
        run: cat Github-Actions-practice.txt
