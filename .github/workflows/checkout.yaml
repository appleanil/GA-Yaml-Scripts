name: Checkout Job

on: workflow_dispatch

jobs:
  Check-out-job:
    runs-on: ubuntu-latest

    steps:
      - name: Simple Check-Out Job
        uses: actions/checkout@v4

      - name: Simple Check-out jobs will see
        run: ls -lrth

      - name: Simplecheck out files
        run: cat Github-Actions-practice.txt
