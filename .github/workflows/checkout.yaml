name: Another Simple Checkout jobs

on: workflow_dispatch

jobs:
  Checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the Repo
        uses: actions/checkout@v4

      - name: show the files
        run: ls -lrth

      - name: Display the file
        run: cat Github-Actions-practice.txt
