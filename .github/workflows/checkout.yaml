name: Checking for Check-out

on: workflow_dispatch

jobs:
  checking-for check-out:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the devops.txt
        uses: actions/checkout@v4

      - name: Show the content file
        run: cat devops.txt
