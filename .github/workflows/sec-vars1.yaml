name: Simple Secrets and Variables in repo-level

on: workflow_dispatch

jobs:
  sec-vars-repo-level:
    runs-on: ubuntu-latest

    steps:
      - name: Showing the Secrets
        run: | 
          echo "My name is $NAME and password is $password" > anil.txt
          echo "my name is $NAME and password is $password.

      - name: Showing the variables
        run: |
          echo "My name is $NAME and My role is $ROLE" > kumar.txt
          echo "My name is $NAME and My role is $ROLE".
