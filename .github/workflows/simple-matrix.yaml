name: Simple Matrix

on: workflow_dispatch

jobs:
  Simple_Matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Simple Matrix
        run: echo "Print OS: ${{ matrix.os }}"
