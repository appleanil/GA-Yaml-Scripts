name: Step Level Time out

on: workflow_dispatch

jobs:
  Step-level-Timeout:
    runs-on: ubuntu-latest
    timeout-minutes: 1

    steps:
      - name: Step level timeout-minutes
        run: |
          echo "Started..."
          sleep 100
          echo "Finished"
